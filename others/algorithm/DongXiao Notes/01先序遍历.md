# 1 Preorder Traversal 

先序遍历的核心：先遍历根节点，再遍历左子树，最后遍历右子树。

实现的方式：**递归**

代码实现：

```C++
void PreOrder(BiTree* T)
{
    if (T == nullptr)
        return;
    cout << T->data << " ";
    PreOrder(T->lchild);
    PreOrder(T->rchild);
}
```

